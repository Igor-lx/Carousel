
export const HOVER_PAUSE_DELAY = 150; // задержка после hover (мс) перед включением паузы автоплея (защита от ненужных срабатываний паузы при дребезге мыши)
export const VISIBILITY_THRESHOLD = 0.2; // процент видимости контейнера для срабатывания паузы автоплея.
export const AUTOPLAY_PAGINATION_FACTOR = 0.2; // доля duration в режиме автоплея, после которой переключается активная точка пагинации.
